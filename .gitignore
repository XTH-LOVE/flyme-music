node_modules
/dist
*.local
*.log
.DS_Store
npm-debug.log*

# 本地环境变量（含 AURORA_AI_API_KEY 等真实密钥，绝不入库）
.env
.env.*
!.env.example

# TypeScript 增量编译缓存（tsc -b 产物）
*.tsbuildinfo

# Rust / Tauri 构建产物
src-tauri/target
src-tauri/gen/android/.gradle
src-tauri/gen/android/build
src-tauri/gen/android/buildSrc/build
src-tauri/gen/android/buildSrc/.gradle
src-tauri/gen/android/app/build
src-tauri/gen/android/app/.cxx

# Android 签名密钥（release 打包产物，绝不入库）
*.keystore
*.jks
signing.properties

# 本地账号数据（含密码哈希）
.aurora-auth.json

# supabase CLI 缓存
supabase/.temp

# 本地密钥 / Cloudflare 缓存（含真实 AI API key，绝不入库）
.dev.vars
.wrangler/

# Cloudflare Pages 拖拽上传包（本地生成）
release-cf/

# 腾讯 EdgeOne 部署缓存（含 ProjectId 等部署元数据）
.edgeone/

# 本地调试截图
.dsh/
