# Flyme Music v0.4.1

### 修复

- **顶部内容不再压到状态栏上。** 应用是 edge-to-edge 的（`enableEdgeToEdge()`），但主题里同时写了「退出 edge-to-edge」，两者冲突导致 WebView 上报的安全区为 0；而 CSS 里底部安全区处理了十几处、顶部一处都没有。现在两者统一，并补上了顶部与左右安全区。
- 主题中冲突的 `windowOptOutEdgeToEdgeEnforcement` 已移除。

### 说明

Android 7.0 及以上。已签名，可直接覆盖安装升级。
