# Flyme Music v0.4.3

### 修复

- **打包版检查更新一直失败。** 检查用的是相对路径 `/api/update/check`，而打包版的页面源是 `tauri.localhost`，相对路径会打到本机地址上，那里没有这个接口。现在打包版改用部署后端的绝对地址，并走 Rust 侧的 `httpFetch`（免 CORS）。

### 说明

Android 7.0 及以上。已签名，可直接覆盖安装升级。
