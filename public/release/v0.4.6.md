# Flyme Music v0.4.6

### 修复

- **通知栏播放器仍然不出现。** 上一版把媒体通知做出来了，但发布时机错了：只在应用切到后台时才启动前台服务，所以在应用内播放时通知栏什么都没有 —— 而 Android 13+ 又要求媒体通知必须由前台服务发出，否则会被丢弃。现在**一开始播放就把服务拉到前台**。
- **切回应用时通知会消失。** `onStart` 里原本会停掉服务 —— 那是通知还是「正在后台运行」时的设计，现在通知就是播放器本身，停掉等于用户一回来控件就没了。已移除。
