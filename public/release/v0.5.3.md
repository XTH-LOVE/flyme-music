# Flyme Music v0.5.3

### 新增

- **通知栏显示歌曲封面。**

  封面图在音源的 CDN 上，是跨域的 —— 由页面去取再交给原生，正是最容易静默失败的那种情况。所以下载放在 Kotlin 里用 `HttpURLConnection` 做，那边根本不存在"源"这回事。

  封面同时写进 `MediaSession` 的元数据，锁屏上也会显示。下载是后台线程，超时 8 秒，失败就退回没有封面 —— **缺封面绝不能让播放控制出问题**。

  只在 URL 真正变化时才重新下载，而且会先清掉上一张，避免慢响应把上一首的封面盖上来。
